# Minimal Lowering Specification for Testing
# Wave S3: Spec-driven lowering

version: "0.1"
target_ir: "0.2"

rules:
  # Only UnitLit for minimal testing
  UnitLit:
    target: CUnitLit
    fields: {}
